let initialState = {
	users: [],
}

const userReducer = (state = initialState, action) => {
  switch (action.type) {
		default:
			return state
	}

}

//actionCreators:

//ThunkCreators:

export default userReducer