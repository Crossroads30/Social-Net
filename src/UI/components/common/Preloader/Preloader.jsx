import preloaderGifUrl from '../../../../assets/gif/colorful-loader-gif-transparent.gif'

const Preloader = () => {
  return <img className='preloader' src={preloaderGifUrl} alt='preloader' />
}

export default Preloader